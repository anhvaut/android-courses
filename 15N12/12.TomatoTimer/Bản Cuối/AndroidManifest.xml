<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ttn.cuongnguyen.tomato">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/unnamed"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Tomato"/>
        <activity android:name=".LichBieu"/>
        <activity android:name=".Clock"/>
        <receiver android:name=".ClockAlarm"/>
        <activity android:name=".Facebook"></activity>
        <activity android:name=".Twitter"></activity>
        <activity android:name=".Setting" />
        <activity android:name=".Login" />
        <service android:name=".ClockMusic"
            android:enabled="true"/>
    </application>

</manifest>